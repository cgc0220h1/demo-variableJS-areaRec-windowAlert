function areaRec() {
    let width;
    let height;
    let area;

    width = prompt("Nhập vào chiều dài");
    height = prompt("Nhập vào chiều rộng");
    area = width * height;
    alert("Diện tích hình chữ nhật là :" + area);
}